import React from 'react';
import styles from './index.module.less';

const Navbar = () => {
  return (
    <header className={styles.navbar}>
      <div className={styles.logo}>影像去云雾</div>
    </header>
  );
};

export default Navbar;
